pipeline {
    agent {
        dockerfile { 
            filename 'Dockerfile.jenkins'
            registryUrl 'https://quay.io/repository/'
            registryCredentialsId 'quay-welltok-mkitawat'
        }
    }
    stages {
        stage('Practice1') {
            steps {
                sh 'whereis python'
                sh 'whereis pip'
                sh 'python --version'
                sh 'ls -la /usr/lib/python*'
                sh 'echo --------------------------------------------------------------------'
                sh 'ls -la /usr/local/lib/python*'
                sh 'pip --version'
                sh 'pip show Django'
            }
        }
    }
}